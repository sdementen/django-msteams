============
Installation
============

At the command line::

    $ easy_install django-msteams

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-msteams
    $ pip install django-msteams
